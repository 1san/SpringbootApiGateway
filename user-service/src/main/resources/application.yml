server:
  port: 8081
spring:
  application:
    name: user-service
  datasource:
    password: root1234
    url: jdbc:mysql://localhost:3306/pocdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        formate-sql: true
    show-sql: true
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

springdoc:
  api-docs:
    path: /user/api-docs
  swagger-ui:
    path: /user/swagger-ui
